---
lang: ru-RU
title: Убираем из логов: Method org.postgresql.jdbc.PgConnection.createClob() is not yet implemented Spring Boot JPA PostgreSQL придложения
date: '2019/02/23 23:36:57'
type: post
tag: null
meta:
  - name: description
    content: заметка
  - name: keywords
    content: spring-boot, postgresql, postgre, JPA, Method PgConnection.createClob() is not yet implemented
---

Если в логах spring-boot / postgres придложения видно приверно такое:

```bash

```

то пофиксить это можно, добавлением следующего конфига в `src/main/resources/application.yaml` файл:

```yaml
spring:
  jpa:
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
```

;)
